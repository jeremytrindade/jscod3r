const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' //operador ternario

/** TERNARIO
 * em primeiro é uma expressão que da sempre true o r false
 * depois tem a ?
 * na primeira é a resposta do true
 * na segunda é a resposta do false
 */

console.log(resultado(7.1))
console.log(resultado(6.7))

